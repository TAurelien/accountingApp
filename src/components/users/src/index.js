'use strict';

module.exports = function setup(options, imports, register) {
	console.log();
	console.log('Registering the users component module ...           [Not yet operationnal]');

	// Register --------------

	register(null);

};