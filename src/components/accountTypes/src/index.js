'use strict';

module.exports = function setup(options, imports, register) {
	console.log('Setting up the accountTypes component module ...    [Not yet operationnal]');

	// Register --------------

	register(null);

};