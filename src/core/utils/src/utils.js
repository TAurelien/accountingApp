/** @module Utils */
'use strict';

module.exports = function setup(options, imports, register) {
	console.log('Setting up the Utils core module ...');

	// Register --------------

	register(null, {

		utils: {}

	});

};